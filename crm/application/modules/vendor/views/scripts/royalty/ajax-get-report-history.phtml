<?php if(sizeof($this->reports)): ?>
<table cellpadding="0" cellspacing="0" border="0" class="dyntable" id="licenses-data-table">
    <thead>
        <tr>
            <th class="head0">Quarter</th>
            <th class="head1">Year</th>
            <th class="head0">Submission Status</th>
            <th class="head1">Submitted on</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($this->reports as $report): ?>
            <tr>
                <td>Q<?php echo $report->quarter; ?></td>
                <td><?php echo $report->year; ?></td>
                <td><?php echo $report->status; ?></td>
                <td><?php echo $report->uploaded_on->format("m/d/Y"); ?></td>
            </tr>        
        <?php endforeach; ?>
    </tbody>
</table>
<?php else: ?>
<div class="notification msgalert">
    No report found
</div>
<?php endif; ?>
